# منظم التنزيلات الذكي - الإصدار المحسن

أداة قوية وتلقائية لتنظيم الملفات تستفيد من الذكاء الاصطناعي (Google Gemini)، والتعلم الآلي، والاستدلال الذكي لترتيب مجلد التنزيلات الخاص بك. يدعم اكتشاف التكرارات، والنسخ الاحتياطي السحابي، وتحليل المحتوى، والمزيد.

## 🚀 الميزات

*   **تصنيف مدعوم بالذكاء الاصطناعي:** يستخدم Google Gemini لفرز الملفات بذكاء بناءً على المحتوى والسياق.
*   **التعلم الآلي المحلي:** تجميع اختياري للملفات باستخدام TF-IDF و DBSCAN.
*   **تحليل المحتوى:** يستخرج النص من المستندات (PDF، DOCX، TXT) والصور (OCR) لتصنيف أفضل.
*   **اكتشاف التكرارات:** يجد التكرارات التامة وشبه التكرارات (الصور) باستراتيجيات معالجة قابلة للتكوين.
*   **النسخ الاحتياطي السحابي:** يقوم بنسخ الملفات المنظمة احتياطيًا تلقائيًا إلى AWS S3.
*   **واجهة برمجة تطبيقات REST:** التحكم ومراقبة المنظم عن بعد عبر HTTP.
*   **واجهة المستخدم الرسومية (GUI) وسطر الأوامر (CLI):** يتضمن واجهة مستخدم رسومية سهلة الاستخدام (Tkinter) وواجهة سطر أوامر قوية.
*   **إعادة التسمية الذكية والإصدارات:** يعالج تعارضات التسمية بذكاء.

## 📋 المتطلبات المسبقة

*   **Python:** الإصدار 3.8 أو أعلى.
*   **نظام التشغيل:** Windows، أو macOS، أو Linux.
*   **مفتاح API لـ Google Gemini:** (اختياري) لميزات الذكاء الاصطناعي.

### التبعيات

حزم Python التالية مطلوبة (بعضها اختياري اعتمادًا على الميزات المفعلة):

*   `google-genai` (لتصنيف الذكاء الاصطناعي)
*   `pillow` (لمعالجة الصور)
*   `piexif` (لبيانات EXIF)
*   `watchdog` (لمراقبة الملفات - اختياري)
*   `aiohttp`، `aiohttp_cors` (لواجهة REST API - اختياري)
*   `scikit-learn`، `numpy` (لتجميع ML - اختياري)
*   `boto3` (للنسخ الاحتياطي AWS S3 - اختياري)
*   `pytesseract` (للتعرف الضوئي على الحروف - اختياري)
*   `schedule` (للجدولة الآلية)

## 🛠️ التثبيت

1.  **استنساخ المستودع:**
    ```bash
    git clone https://github.com/yourusername/downloads-organizer.git
    cd downloads-organizer
    ```

2.  **تثبيت التبعيات:**
    يوصى باستخدام بيئة افتراضية.
    ```bash
    python -m venv venv
    source venv/bin/activate  # على Windows: venv\Scripts\activate
    pip install google-genai pillow piexif schedule

    # التبعيات الاختيارية
    pip install aiohttp aiohttp_cors scikit-learn numpy boto3 pytesseract watchdog
    ```

3.  **Tesseract OCR (اختياري):**
    إذا كنت ترغب في استخدام OCR للصور، فقم بتثبيت Tesseract OCR على نظامك وتأكد من وجوده في مسار النظام (PATH).

## ⚡ البدء السريع

### واجهة المستخدم الرسومية (GUI)

ببساطة قم بتشغيل البرنامج النصي بدون وسائط لتشغيل واجهة المستخدم الرسومية:

```bash
python Downloads-Organizer.py
```

1.  **اختر المجلد المستهدف:** اختر المجلد الذي تريد تنظيمه (الافتراضي: التنزيلات).
2.  **تكوين الخيارات:** قم بتمكين الذكاء الاصطناعي، أو التعلم الآلي، أو النسخ الاحتياطي السحابي حسب الحاجة.
3.  **انقر فوق "Organize Now" (نظم الآن):** اجلس وشاهد ملفاتك يتم فرزها!

### واجهة سطر الأوامر (CLI)

قم تشغيل المنظم في الوضع غير المرئي للنصوص البرمجية أو الخوادم:

```bash
# تشغيل أساسي
python Downloads-Organizer.py --no-gui

# مع هدف محدد ومفتاح الذكاء الاصطناعي
python Downloads-Organizer.py --no-gui --target "/path/to/folder" --api-key "YOUR_KEY"

# تشغيل تجريبي (محاكاة فقط)
python Downloads-Organizer.py --no-gui --dry-run
```

## 📂 هيكل المشروع

*   `Downloads-Organizer.py`: البرنامج النصي للتطبيق الرئيسي الذي يحتوي على كل المنطق (واجهة المستخدم، المحرك، قاعدة البيانات، إلخ).
*   `test_duplicate_filenames.py`: اختبارات الوحدة للتحقق من منطق التعامل مع التكرارات.
*   `.organizer_cache.db`: (يتم إنشاؤه) قاعدة بيانات SQLite لتخزين البيانات الوصفية للملفات مؤقتًا.
*   `.organizer_config.json`: (يتم إنشاؤه) ملف التكوين الذي يخزن تفضيلات المستخدم.
*   `organizer.log`: (يتم إنشاؤه) ملف سجل لتتبع العمليات والأخطاء.

## ⚙️ التكوين

يتم حفظ التكوين تلقائيًا في `.organizer_config.json` في المجلد المستهدف الخاص بك. يمكنك أيضًا تعديل الإعدادات عبر علامة تبويب "Settings" في واجهة المستخدم الرسومية أو وسائط CLI.

**الإعدادات الرئيسية:**
*   `duplicate_strategy`: كيفية التعامل مع التكرارات (`skip`, `rename`, `move`, `smart_merge`, `version`).
*   `cloud_provider`: مزود التخزين السحابي (يدعم حاليًا `aws_s3`).
*   `schedule_interval`: تكرار عمليات التشغيل الآلي (`hourly`, `daily`, `weekly`).

## 🤝 المساهمة

المساهمات مرحب بها! يرجى التأكد من توثيق أي كود جديد بشكل جيد باتباع معايير سلاسل التوثيق الموجودة (باللغة العربية).
